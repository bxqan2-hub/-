# 2026-09-11 出口探测回归与注册指纹核对

## 结论与交付状态

- 本轮确有本地实现问题：Selenium 出口探测的并行请求被固定在 2 秒，失败后只直达最后一个接口；运行配置又把单接口超时设为 4 秒。现场同一代理在 4 秒预算下失败，8 秒预算下约 4.1–4.4 秒返回 HTTP 200。此前把这类失败一概归因于代理的结论过强，本报告予以纠正。
- 同时存在真实出口冲突和漂移，延长等待不会消除这些问题。保留原子占用、冷却、同窗出口复核和不一致即终止，不使用预检 IP 冒充浏览器实测。
- 原路径修复及回归已完成；3 个独立 Roxy Profile 的真实预检、窗口内复核均通过，且已清理。本次未注册新账号，不把探测成功写成注册成功。
- `.env` 中已有 `IP_GEO_TIMEOUT` 已通过现有配置 API 从 4 调到 8，响应确认热加载。自动重启操作被执行环境拦截，复核 5001 仍由原 PID 45552 监听；新的 Python 探测实现需要手动重启服务后生效。运行中的配置变更与源码加载状态分开记录。
- 当前 API 与账号文件均只保留 12 个 9 月 2 日成功账号，全部标记有试用资格，无不具资格的对照样本。按用户补充要求，比较仅使用注册时的指纹/IP，不纳入注册结束后的资格检测地区。

## 原因优先级与逐项证据

| 初始概率 | 假设 | 核对结果 / Finding | Path |
| --- | --- | --- | --- |
| 高 | 代理池不同入口对应同一出口，或出口处于冷却 | 21:49 保留失败中 22 条属于冲突；22:14 最新记录也包含冲突。1000 条候选 URL/用户名各不相同，不等于有 1000 个不同出口 | `core/roxybrowser_client.py::open_profile`；`config/proxy.py::reserve_registration_exit_ip` |
| 中 | 本地快速探测预算过短、接口选择不一致 | 同一代理 HTTP 对照已重现 4 秒超时 / 8 秒成功；旧浏览器 2 秒并行加末端单接口的路径还会漏掉有效接口。确认需要修复 | `core/browser_exit_geo.py::probe_selenium_driver_exit_geo`、`probe_proxy_exit_geo` |
| 中 | 代理在预检与实际窗口之间换出口 | Job 501 的预检/浏览器实测是不同 IP；Job 502 首个候选又命中 501 已占用 IP。属于真实路由问题，不是 MFA | `core/roxy_registration.py::_verify_registration_exit_geo` |
| 低 | 最新四条失败由密码或 MFA 导致 | Job 482/486/501/502 均在出口前置门槛停止，尚未进入邮箱提交、密码设置或 MFA | `注册日志/79ba52fe-f832-42e7-bcb9-27ee3a1a4f1c.log` 等四条记录 |

### 最新 22:14 批次留存的四条失败

| Job | 证据日志 | 实际失败阶段 |
| --- | --- | --- |
| 482 | `注册日志/79ba52fe-f832-42e7-bcb9-27ee3a1a4f1c.log` | 候选依次为未取到 IP、已占用 IP、未取到 IP；未创建环境 |
| 486 | `注册日志/e72fc01e-853f-4fe7-85ae-7a002bd71d8b.log` | 预检取得 JP 出口，窗口内旧探测返回空；未提交注册邮箱 |
| 501 | `注册日志/1a57e5ba-d9bc-49bc-b39f-57bbad656265.log` | 窗口实际出口与预检出口不同，正确终止 |
| 502 | `注册日志/c3205bec-4f16-4883-af04-a131e4456370.log` | 第一个候选出口冲突；更换后预检成功，但窗口内旧探测为空 |

22:41 核对时保留 188 条任务：138 failed、49 cancelled、1 stopped、0 active。当前成功任务记录未在此列表中，禁止用这个留存集合推导整批成功率为零。

### 对照实验与修复后的真实浏览器验证

- 只取代理池中 3 个固定候选，保持同一候选的凭据，分别测 `ipinfo.io/json`、`ipwho.is/`。直接代理请求使用明确代理、`trust_env=False`、TLS 校验开启、不跟随重定向；未打印用户名、密码或完整代理 URL。
- 4 秒预算两轮：P1/P2 的 ipinfo 请求均在约 4.01 秒超时，ipwho 约 3.30–3.90 秒成功；P3 两接口约 3.57–3.78 秒成功。
- 8 秒预算：P1/P2 两接口约 4.13–4.40 秒成功，P3 约 3.64–3.86 秒成功。每个候选的两接口出口一致，多轮所观察出口也一致。此结果证明短超时存在误判，不代表代理池全体稳定。
- 新代码现场：3 个新建独立 Profile、3 个不同出口，均 `preflight_ip == browser_ip`、`verification_source=browser_context`；原标签页均恢复，环境清理均返回成功。全流程含创建/清理分别 26.18、25.97、22.11 秒。
- 浏览器实测使用内核 153，3 次均请求随机指纹，随机 OS 本次均为 macOS。它们仅做出口验证，不参与试用资格样本比较。
- 脱敏原始诊断保存在 Git 忽略目录：`run/proxy-endpoint-audit-20260911.json`、`run/proxy-browser-regression-20260911.json`。IP 明细不提交 Git。

## 其他注册失败与邮箱实查

- 21:49 批次另外的 12 条失败分开归类：8 条收码等待超时、1 条邮箱接口 ReadTimeout、1 条验证码页面出现上游 JSON/HTTP 503、2 条登录/验证码页面为空或中转停滞。未合并成 MFA 失败。
- 对 Job 394、399、401、402、404、405、406、407、409 的对应邮箱做只读实查；邮箱列表接口 `items` 为准，邮箱路径与任务目标精确匹配；8 个邮箱现有邮件且详情接口成功，现有解析器均取到 6 位验证码，394 当前为空。
- 8 封邮件的 `received_at` 大多早于日志进入等待约 2–7 秒。对 Job 399 按原登录起始时间重放现有 `_fetch_yangyang_otp`，当前正常取码；编码与未编码 `@` 路径结果一致。
- 上述事后实查只证明现在邮件存在且当前解析可用。注册当时各次列表响应缺少保存，尚未证实当时 API 是否已经暴露该邮件；供应商索引延迟、历史读取瞬态仍待带时间线的复现。未凭猜测修改邮箱适配器或取消时间过滤。
- 邮箱证据仅保存 ID、时间、状态、解析是否成功，不保存邮件正文、验证码或私有取码 URL：`run/mail-failure-audit-20260911-late.json`。

## 注册成功账号的指纹与 IP

| 注册时特征 | 有试用资格样本 | 无试用资格样本 |
| --- | --- | --- |
| 可用账号数 | 12，均为 9 月 2 日记录 | 0 |
| 注册出口国家 | JP 5；VN 7 | 无样本 |
| 不同注册出口 IP | 11；账号 8 与 11 使用过同一 IP | 无样本 |
| 不同 Profile ID | 12 | 无样本 |
| 返回的浏览器内核 | 全部 152 | 无样本 |
| OS / UA / Canvas / WebGL / 硬件 / 语言等 | 旧账号文件未保存 | 无样本 |

- 12 个不同 Profile ID 只证明环境标识不同，不足以证明底层指纹参数各异。
- 按这 12 个 Profile ID 回扫现有 `注册日志`，没有找到对应历史日志；未从其他不相关目录恢复或推测指纹。
- 账号明细仅在本地脱敏诊断 `run/trial-fingerprint-audit-20260911.json` 中列出账号 ID、注册 IP、国家、Profile ID 与内核，不含账号邮箱、密码、Token 或 TOTP Secret。
- “同一 IP 的两个账号均有资格”只是当前旧样本的事实，不证明复用 IP/指纹会产生资格。浏览器 152 与当前 153 也没有同批次、同条件的正反样本，暂不作因果结论。
- 未实施用户提到的后续指纹复用功能。仍保持独立 Profile、随机指纹和出口隔离的当前实现。

## 上游对照、密码与 MFA 边界复核

- 修改前按 `docs/UPSTREAM_PROJECT_INDEX.md` 重新读取锁定 `Torin-x/GPT-utral-platform@68a1f8faede7e41f10ac5f9af267465fa61d0e3d` 的 `vendor/turb_gpt_free_register/core/roxy_registration.py`、`core/account_export.py`。前者 SHA-256 为 `454a5a84a75613e5617a54491744c2cd9f595d05af64fab824e4de880219ccf5`，后者为 `985b99e669711d3de50dd6072ba83f660ac74d5ea156bf84f829c8fb258d0547`。版本不变，没有覆盖 vendor。
- 上游没有本地这个出口探测 helper；修复落在本地原路径，保留其注册会话连续性和 MFA enroll → activate 顺序。
- 密码：检查 `core/roxy_registration.py::_fill_password_page_if_present` 与 `core/account_export.py::_setup_password_with_driver`；确认终态之后才提交 password checkpoint，邮箱挑战失败不会标记密码已配置。此轮未修改密码状态机。
- MFA：检查 `core/account_export.py::_setup_totp_with_driver` 与调用方；目标邮箱匹配、显式 Token、同窗 Cookie/代理继续保留；enroll 返回有效 Secret/session_id，activate 的 `success is True` 后才返回 Secret 并写 checkpoint。只读 Token 校验与写操作状态仍分开。
- 越权与泄露：探测不发送账号 Token/Cookie，不打印代理凭据或异常原文；只记录接口主机、HTTP 状态、异常类型/curl code。BAT 仅请求本机 `/login` 并比对已有页面标识，不发送 `.env` 授权码、不读取账号 API、不跟随重定向。
- 全量测试包含账号错配、密码终态、激活失败不落 Secret、Token 刷新、停止与恢复回归；这属于回归验证，不替代新批次远端注册验证。

## “只改不堆”八项自检

1. **修改的已有代码**：`core/browser_exit_geo.py::normalize_browser_exit_geo/probe_proxy_exit_geo/probe_selenium_driver_exit_geo`；`core/roxybrowser_client.py::RoxyBrowserClient.open_profile` 的失败诊断；`config/browser.py::IP_GEO_TIMEOUT`；`webui/config_editor.py` 同一配置项帮助；`start-webui.bat` 原占用端口分支。删除旧 2 秒并行路径与只测末端接口的回退，不增加第二套探测器。
2. **新增代码**：生产代码没有新增函数/类/模块/配置字段，仅在既有函数增加脱敏诊断、失败 payload 检查。测试文件新增超时后换接口、停止清理、有限次数、日志脱敏和假 WebUI/重定向/不发送凭据用例；替换旧并行探测测试，旧实现与旧测试名已删除。新增本报告是本轮证据记录，不是第二套实现。
3. **搬迁项**：无，没有复制或移动生产实现。
4. **配置链路**：没有新增配置。已有 `WebUI /api/config → update_config/.env → config.browser.IP_GEO_TIMEOUT → _probe_settings → 预检/窗口内探测`；默认值与当前运行配置均设为 8 秒。每个接口等待上限增加是有意权衡，候选和尝试次数仍有界。
5. **死引用回扫**：以下命令无输出（rg 的无匹配退出码为 1）；范围为被替换的生产和测试文件，历史报告作为历史证据保留。

   ```powershell
   git grep -n -E 'parallel_timeout_ms|fallback_endpoint|WEBUI_HTTP|test_selenium_probe_queries_all_endpoints_in_one_parallel_call|test_selenium_probe_uses_one_direct_fallback_when_parallel_fetch_is_empty' -- core/browser_exit_geo.py start-webui.bat tests/test_registration_browser_exit_geo.py tests/test_webui_startup.py
   ```

6. **diff 统计**：生产代码 `+34 / -87`；生产与测试合计 `+132 / -104`；含本报告和上游索引的本次总 diff 为 `+250 / -104`。BAT 上一提交存在重复成功分支、空操作和仅凭 200/302 的误识别，确实不符合收缩要求，本次已在同一分支收缩并补测试。
7. **验证**：定向 `62 passed, 1 warning, 4 subtests passed`；编译与 `git diff --check` 通过；完整运行环境自检通过。真实 BAT 已运行服务分支退出 0、PID 保持 45552；普通 HTTP 200/302、超时占用端口均不误开第二服务、不发送授权码。全量命令：

   ```powershell
   .\.venv\Scripts\python.exe -m pytest -q --disable-warnings --deselect=tests/test_extract_center_cleanup.py::ExtractCenterCleanupTests::test_protocol_payment_routes_and_runtime_are_removed
   ```

   最后 5 行原始输出：

   ```text
   ........................................................................ [ 83%]
   ........................................................................ [ 91%]
   ........................................................................ [ 98%]
   ............                                                             [100%]
   959 passed, 1 deselected, 1 warning, 61 subtests passed in 34.90s
   ```

8. **未做 / 存疑**：
   - 自动重启被拦截，现场进程尚未加载新探测代码；手动退出本站服务后再运行 BAT，才是交付的运行生效点。
   - 真实验证仅覆盖 3 个出口与 Profile；本轮未新增注册账号，也未验证代理池全部 1000 个候选。
   - 8 个现有收件箱有邮件，但当时的接口可见时间未定；登录空白/503 仍属独立页面/上游故障，未添加盲目重试。
   - 缺少无资格账号和完整历史指纹，注册特征比较只有单组描述，不给出最优指纹或因果承诺。
   - 排除的唯一测试断言 `integrations/paypal_agreement_protocol/` 不存在；该目录与 `tests/test_payment_form_memory.py`、`tests/test_paypal_protocol_proxy_formats.py` 均为本轮开始前已有未跟踪内容，保持原样，不删除、不提交。其余全量测试正常。
   - requests 的字符集检测依赖 warning 是既存环境提示，本轮未改依赖。
   - 进程间共享出口 lease 尚未实现，CLI 与 WebUI 注册继续串行使用；没有用本次探测修复改变此边界。
