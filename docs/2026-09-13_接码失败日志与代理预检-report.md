# 2026-09-13 接码失败日志与代理预检报告

## 结论

最新 Codex 补跑在短信接口之前失败。Roxy Profile 已创建并打开，但浏览器访问授权页返回 `net::ERR_PROXY_CONNECTION_FAILED`。

## 原因分级与证据

- **高概率：本地代理端口失效。** `注册日志/codex-retry-cubit_betel9d@icloud.com.log` 记录了 `CODEX_LOCAL_PROXY=http://127.0.0.1:7890`，随后出现 `ERR_PROXY_CONNECTION_FAILED`；端口检查未发现 `127.0.0.1:7890` 监听。
- **中概率：可见的替代入口未形成可用出口。** `127.0.0.1:10809` 有监听，但经 HTTP/SOCKS 请求至 ChatGPT 超时，因此未直接替换配置。
- **低概率/历史：HeroSMS 凭据问题。** `codex-retry-tammie_hobbslcv@hot-shot.com.log` 曾出现 `HeroSMS HTTP 401 BAD_KEY`；当前 `.env` 使用 SMSBower，且国家/价格 API 返回 200，和本次失败阶段无关。

## Finding → Path → 修复

1. **Finding：Codex 补跑在打开 Profile 前没有验证专用代理。**
   - Path：`core/roxy_codex_oauth.py::run_roxy_codex_oauth` → `_run_roxy_codex_oauth_once`。
   - 修复：复用 `core.browser_exit_geo.probe_proxy_exit_geo`，在新 Profile 创建前执行真实出口预检；失败返回 `stage=proxy_transport`，不再创建无效 Profile，也不消耗短信。
2. **Finding：复用注册窗口时不应重复探测。**
   - Path：`run_roxy_codex_oauth(..., reuse_existing_profile=True)`。
   - 修复：仅新建 Codex Profile 预检；注册流程已有窗口内出口复核。

## 上游对照

上游锁定 commit `68a1f8faede7e41f10ac5f9af267465fa61d0e3d` 的 `vendor/turb_gpt_free_register/core/roxy_registration.py` 在新 Profile 前执行代理出口预检。本地 Codex 补跑此前绕过该边界，本次补齐同一检查点，保留本地 Profile、Cookie、Token 和 OTP 生命周期约束。

## 验证

- `tests/test_roxy_codex_proxy_preflight.py`：覆盖失效代理提前终止、复用窗口跳过重复预检。
- `tests/test_webui_helper_regressions.py -q`：33 passed。
- `git diff --check`：通过。

## 待处理

需要恢复 `127.0.0.1:7890` 对应代理服务，或提供经过出口预检的可用代理地址；`10809` 当前探测超时，不作为自动回退目标。
