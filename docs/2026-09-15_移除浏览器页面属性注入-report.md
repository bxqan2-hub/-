# 2026-09-15 移除浏览器连接时的页面属性注入

## 结果与范围

- 用户要求先移除该功能并自行测试；后续是否回退由用户决定。修改前 HEAD 为 `4f98376f0c1caee46c59a1f3f015cb6cf4c024b3`，只用 Git 保存回退历史，没有创建项目/源文件备份。
- 删除 Selenium 连接后对 webdriver 标记、chrome runtime 对象及通知权限查询的页面注入脚本，同时删除两处调用与对应日志。不是只隐藏日志，也不是关闭整个人类化行为配置。
- 共享 driver 构建路径被注册、独立 Codex 和补密码/2FA 调用；这些路径今后都不再执行这段本地注入。Roxy 自身的指纹能力及其他平台驱动未变更。
- AST 对比确认原模块仅 driver 构建函数改变，并删除一个 helper；其余函数/类保持一致。流量缓存、资源拦截、节流参数、预算、代理池、TLS 校验、重试次数均保持。

## 上游对照与安全复核

- 已先读 `docs/UPSTREAM_PROJECT_INDEX.md`，重新获取锁定版本 `68a1f8faede7e41f10ac5f9af267465fa61d0e3d` 的 `vendor/turb_gpt_free_register/core/roxy_registration.py`，HTTP 200，SHA-256 `454a5a84a75613e5617a54491744c2cd9f595d05af64fab824e4de880219ccf5`。
- 上游没有该本地页面属性注入 helper；只移除用户点名功能，不用上游文件覆盖本地 driver 路径和启动重试。
- Finding：连接 driver 时主动修改页面属性是这条日志的来源；本次按用户要求删除，尚未宣称注册成功率因此提高。
- Path：`core/roxy_registration.py` 的 driver 创建/返回之间两处调用及其脚本函数。
- 密码与 MFA 的邮箱匹配、同一浏览器 Cookie/代理、显式新 Token、密码确认后 checkpoint、enroll/activate 成功后保存 Secret 未变更；相关回归通过。未新增凭据输出或远端写操作。

## 验证与运行切换

- 新增 driver 连接回归覆盖：Roxy 返回驱动路径、本机默认驱动、Remote WebDriver。均保留 eager 页面加载、performance 日志和连接参数；连接过程不得调用 CDP 页面脚本注入或 execute_script，即使原人类化行为开关为 true。
- 红绿验证：旧实现 3 failed / 1 passed / 19 deselected；移除后相关合并回归 485 passed / 305 subtests。
- pytest 使用既有 conftest 的隔离数据库；没有在真实运行数据上另行 create_app 做测试。
- 运行切换：检测到无注册/安全设置任务，仅账号 361 套餐查询状态自 02:01:26 起停留超过 9 分钟。已告知用户其会被标记中断，随后通过既有 start-webui.bat 5002 重启；新版 listener PID 29224，端口仍为 127.0.0.1:5002。该套餐查询可重新发起。
- 未主动创建注册账号、发送 OTP、购买手机号或变更远端密码/MFA；真实注册效果由用户后续测试。

## R1–R8 交付自检

1. **修改的已有代码**：`core/roxy_registration.py:_build_driver`，删除两个连接分支中的页面属性注入调用；删除该注入 helper、JS 和对应日志。
2. **新增的代码**：`tests/test_roxy_registration_session_recovery.py:RoxyRegistrationSessionRecoveryTests.test_driver_connection_leaves_page_scripts_untouched`。此前 grep driver 构建只找到调用方 mock，没有覆盖实际构建路径；新增测试约束本次删除行为，没有新增业务函数/备用路径。被替代的注入实现已删除。
3. **搬迁项**：无，没有复制实现到其他文件。
4. **新增配置项链路**：无；既有 `ENABLE_HUMANIZE_BROWSER_ACTIONS` 仍供点击/滚动等原有读取点使用。
5. **死引用回扫**：

   ```powershell
   rg -n '_apply_browser_automation_mask|已注入浏览器自动化特征弱化脚本|注入自动化特征弱化脚本失败' core config tests docs .env.example --glob '!2026-09-15_移除浏览器页面属性注入-report.md'
   ```

   输出为空（退出码 1）；仅排除本报告里的检索命令本身。

6. **Diff 统计**：总计 `+104 / −31`；运行实现净删除 31 行，测试新增 37 行，文档记录新增 67 行；没有平行实现。
7. **测试**：

   ```powershell
   .\.venv\Scripts\python.exe -X utf8 -m pytest -q --disable-warnings tests/test_roxy_registration_session_recovery.py tests/test_roxy_registration_otp_recovery.py tests/test_roxy_codex_otp_polling.py tests/test_roxy_codex_proxy_preflight.py tests/test_account_security_extension.py tests/test_security_checkpoint.py tests/test_twofa_registration.py tests/test_browser_traffic.py
   ```

   最后 5 行原始输出（完整日志位于忽略目录 `run/browser-native-driver-tests-20260915.log`）：

   ```text
........................................................................ [ 60%]
........................................................................ [ 74%]
............................................................................................ [ 93%]
..............................   [100%]
485 passed, 1 warning, 305 subtests passed in 20.23s
   ```

8. **未做的 / 存疑的**：
   - 未进行真实账号注册测试，不推断移除后的网站判定或成功率；用户准备自行测试。
   - 已存在依赖 warning，本次没有修改依赖。
   - 三个任务前已有未跟踪路径保持原样：`integrations/paypal_agreement_protocol/`、`tests/test_payment_form_memory.py`、`tests/test_paypal_protocol_proxy_formats.py`；不纳入提交。
   - 回退应仅撤销本次提交，保留此前 TLS、邮箱、密码/2FA 和 5002 启动修复。
